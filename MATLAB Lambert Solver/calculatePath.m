function path = calculatePath(trajectory, times, planets, dt)
    % Given the vector of optimal planets to visit and the times of the
    % interactions, create a matrix of the spacecraft's x and y positions
    % at discrete time steps dt
    % return path as matrix with x values in first column, y values in
    % second column

end